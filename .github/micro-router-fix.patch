diff --git a/native/pcb-board-packer/src/micro_router.rs b/native/pcb-board-packer/src/micro_router.rs
index 365b851..de7b205 100644
--- a/native/pcb-board-packer/src/micro_router.rs
+++ b/native/pcb-board-packer/src/micro_router.rs
@@ -369,6 +369,7 @@ fn resolve_endpoint(endpoint: &str, primitives: &[&Primitive], config: &MicroRo
             .filter(|primitive| primitive.placements.iter().any(|p| p.designator.as_ref() == designator))
             .flat_map(|primitive| {
                 let layer = primitive_layer(primitive, config);
+                let prefix = prefix.clone();
                 primitive.connection_points.iter().filter_map(move |point| {
                     let net = point.net.as_ref()?;
                     point.reference.starts_with(&prefix).then(|| RouteEndpoint {
